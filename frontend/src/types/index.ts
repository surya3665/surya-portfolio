export interface Project {
    title: string
    description: string
    tech: string[]
    github: string
    live: string
  }
  
  export interface Skill {
    name: string
    category: string
  }
  
  export interface ContactForm {
    name: string
    email: string
    message: string
  }